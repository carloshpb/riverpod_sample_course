name: riverpod_sample_course
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.3.1
  firebase_core: ^3.6.0
  flutter_dotenv: ^5.2.1
  flutter_riverpod: ^2.5.3
  freezed: ^2.5.7
  freezed_annotation: ^2.4.4
  http: ^1.2.2
  json_annotation:
  riverpod_analyzer_utils: 0.5.4 # Deve ficar nessa versão, por agora (11/2024)
  riverpod_annotation: ^2.5.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13 # Sempre necessário quando for utilizar alguma lib 'generator' ou 'builder'
  custom_lint:
  flutter_lints: ^4.0.0
  json_serializable:
  riverpod_generator: 2.4.3 # Deve ficar nessa versão, por agora (11/2024)
  riverpod_lint:

flutter:
  uses-material-design: true
